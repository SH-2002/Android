<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/fab_margin"
    android:layout_marginTop="@dimen/fab_margin_half"
    android:layout_marginBottom="@dimen/fab_margin_half"
    android:layout_marginEnd="@dimen/fab_margin"
    android:background="@android:color/transparent"
    android:paddingStart="@dimen/fab_margin_half"
    android:paddingEnd="@dimen/fab_margin_half">

    <TextView
        android:id="@+id/userMsg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:background="@drawable/user_msg_background"
        android:fontFamily="@font/roboto_slab_thin"
        android:paddingStart="@dimen/textStartPadding"
        android:paddingTop="@dimen/textPadding"
        android:paddingEnd="@dimen/textStartPadding"
        android:paddingBottom="@dimen/textPadding"
        android:text="@string/app_name"
        android:textAlignment="viewStart"
        android:textColor="@color/gray"
        android:textSize="@dimen/userMsgTextSize" />

</RelativeLayout>